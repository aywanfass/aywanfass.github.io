import React from "react";

export default function Footer() {
  return (
    <footer className="py-3" aria-label="Pied de page">
      <div className="container text-center">
        <small className="text-muted">
          Besoin d’une version multi‑pages (CV, Réalisations, Contact) avec
          router ? Je peux l’ajouter.
        </small>
      </div>
    </footer>
  );
}
